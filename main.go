package main

import (
	"fmt"
)

func main() {
	fmt.Println("Starting go service...")
	// Gowiki Handlers
	// http.HandleFunc("/view/", gowiki.MakeHandler(gowiki.ViewHandler))
	// http.HandleFunc("/edit/", gowiki.MakeHandler(gowiki.EditHandler))
	// http.HandleFunc("/save/", gowiki.MakeHandler(gowiki.SaveHandler))
	// log.Fatal(http.ListenAndServe(":8080", nil))

	// GoTodo Main Func
	// gotodo.Run()
}
